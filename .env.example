# Copy to .env.local for local overrides (see README).
# Clerk: enable Email/Password + Google; allowlist /login, /sign-up, /sso-callback.
VITE_CLERK_PUBLISHABLE_KEY=
VITE_API_URL=http://localhost:8000

# Optional Stripe Payment Links for public pricing CTAs (fallback: app signup URL).
# VITE_STRIPE_PAYMENT_LINK_BASIC=
# VITE_STRIPE_PAYMENT_LINK_GROWTH=
