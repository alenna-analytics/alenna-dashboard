# alenna-analytics-dashboard — AI & developer guide

Vite + React + Clerk. Providers: theme, language, TanStack Query. API calls use `src/lib/api.ts` against **alenna-analytics-api**; no direct database access.
