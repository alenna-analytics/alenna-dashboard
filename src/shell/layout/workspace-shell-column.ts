/** Horizontal padding shared by main content. Body fills the remaining shell width. */
export const WORKSPACE_SHELL_COLUMN_CLASS = 'w-full px-4 lg:px-5'

/** Full-bleed chrome (header, sync banner) — same inset, no max-width. */
export const WORKSPACE_SHELL_CHROME_INSET_CLASS = 'w-full px-4 lg:px-5'
