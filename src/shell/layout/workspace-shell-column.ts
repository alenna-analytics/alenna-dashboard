/** Max-width + horizontal padding shared by main content so both edges align when centered. */
export const WORKSPACE_SHELL_COLUMN_CLASS =
  'mx-auto w-full max-w-[1740px] px-4 lg:px-5'

/** Full-bleed chrome (header, sync banner) — same inset, no max-width. */
export const WORKSPACE_SHELL_CHROME_INSET_CLASS = 'w-full px-4 lg:px-5'
